appId: ai.january.partner.demo
name: Meal scan and correction
tags:
  - fixture
---
- runFlow: ../bootstrap.yaml
- extendedWaitUntil:
    visible:
      id: tab-scan
    timeout: 15000
- tapOn:
    id: tab-scan
- tapOn:
    id: scan-sample
- assertVisible:
    id: scan-preview
- tapOn:
    id: scan-analyze
- extendedWaitUntil:
    visible:
      id: scan-results
    timeout: 15000
- assertVisible: Fixture breakfast
- scrollUntilVisible:
    element:
      text: Fixture oatmeal
    direction: DOWN
- scrollUntilVisible:
    element:
      id: scan-correct
    direction: DOWN
- tapOn:
    id: scan-correct
- extendedWaitUntil:
    visible:
      id: scan-correction-input
    timeout: 15000
- tapOn:
    id: scan-correction-input
- inputText: This was lentils
- scrollUntilVisible:
    element:
      id: scan-correction-submit
    direction: DOWN
- tapOn:
    id: scan-correction-submit
# The corrected meal name heads the sheet content, above the scroll position
# the correction button was tapped from.
- scrollUntilVisible:
    element:
      text: Corrected breakfast
    direction: UP
    timeout: 30000
